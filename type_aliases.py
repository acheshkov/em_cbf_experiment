import pandas as pd
from typing import List, Tuple, Dict, Optional, Any


Path = str
ProjectName = str
ModelName = str
Vector = List[float]
Dataset = pd.DataFrame
InferenceResults = pd.DataFrame
RangesDataset = Dataset
EmosVectorsDataset = Dataset
SynthDataset = Dataset